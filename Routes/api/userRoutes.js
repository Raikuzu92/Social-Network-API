// const express = require('express');
// const router = express.Router();
// const User = require('../Controllers/userController'); // Adjust the path to where your User model is located

// // Create a new user
// router.post('/users', userController.createUser);

// // Get all users
// router.get('/users', userController.getAllUsers);

// // Get a single user by ID
// router.get('/users/:id', userController.getUserById);

// // Update a user by ID
// router.put('/users/:id', userController.updateUserById);

// // Delete a user by ID
// router.delete('/users/:id', userController.deleteUserById);

// // Add a friend to a user's friend list
// router.post('/users/:userId/friends/:friendId', userController.addFriend);

// // Remove a friend from a user's friend list
// router.delete('/users/:userId/friends/:friendId', userController.removeFriend);

// module.exports = router;